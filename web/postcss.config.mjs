// Next only reads a PostCSS config from an app's own root, so this file has to
// exist here — but the pipeline itself is shared with the client portal.
export { default } from "../shared/web/postcss.config.mjs"
