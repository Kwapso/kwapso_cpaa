{
  "name": "kwapso-data-ops",
  "version": "0.1.0",
  "private": true,
  "description": "kwapso's data-ops worker: bulk data import (and, next, the AI agent brain).",
  "scripts": {
    "dev": "wrangler dev --env staging --port 8790 --inspector-port 9233 --local --persist-to ../../.dev-state",
    "test": "vitest run --config ../../vitest.workers.config.ts",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260830.1",
    "typescript": "^5.7.3",
    "vitest": "^4.1.8"
  }
}
