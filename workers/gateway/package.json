{
  "name": "kwapso-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "kwapso's front door: serves the screens, routes /api to the domain workers.",
  "scripts": {
    "test": "vitest run --config ../../vitest.workers.config.ts",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260830.1",
    "typescript": "^5.7.3",
    "vitest": "^4.1.8"
  }
}
