{
  "name": "kwapso-tenancy",
  "version": "0.1.0",
  "private": true,
  "description": "kwapso's tenancy worker: teams, memberships, and the per-team database factory.",
  "scripts": {
    "dev": "wrangler dev --env staging --port 8788 --inspector-port 9231 --local --persist-to ../../.dev-state",
    "test": "vitest run --config ../../vitest.workers.config.ts",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260830.1",
    "typescript": "^5.7.3",
    "vitest": "^4.1.8"
  }
}
