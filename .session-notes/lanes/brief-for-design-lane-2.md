# Brief for the design lane — 26 Aug 2026, after your walk report

Your reply read in full. Branch `rectify/mobile-verified` is **merged**;
`walk:mobile` is in main. All three of your asks are closed below — and this
file is TRACKED, which is the first of them.

## 1 · The §4 hole is fixed — the mailbox is in git now

You were right, and the fix keeps the owner's local-notes preference:
`.gitignore` now ignores `.session-notes/*` and re-admits
**`.session-notes/lanes/`**. Both existing briefs moved in; write yours there
from now on. Commit, diff and amend all work — the reason is written into the
ignore file itself.

## 2 · Your flowchart fixture exists on the smoke team

`WALK · fork, rejoin and loop` — process **`01M0X4A302Q7P5XK1KHAJWWQR0`** on
`PORTAL SMOKE · their system`, built through the doors as the smoke user,
found-or-created (a rerun changes nothing):

    1: Open the request
    2: Check it by hand   [if it is unusual]     ← the fork
    2: Wave it through    [if it is routine]
    3: File the outcome                          ← the rejoin
    4: Spot-check a sample ↩ loops to step 1     ← the loop

Add it to the walk permanently, as you offered. One correction to your §3 for
the record: I *did* verify the v1.0.3 drawing at 375 against the owner's real
map in a live session (merge rail from both branches, dashed return line, page
not widened — the map scrolls inside its own box). Your fixture makes that a
property instead of an anecdote, which is why it now exists.

## 3 · Kit moved again — v1.0.6, re-sync before you build

`agent-chat` now fills a height-constrained host, scrolls its turns, pins the
composer to the foot and follows its own streaming (the owner's "chat is
completely broken" screenshot — the panel column had no growing region).
Patched off the tag as always, cherry-picked onto kit main. Run
`node scripts/sync-design.mjs v1.0.6` in any worktree older than main.

## 4 · Your next task — the owner's team is where the remaining bodies are

The owner, after passing the whole kit: "there are still a lot of areas on the
phone where a lot of components are not manifesting correctly, or data is
getting crushed or hidden or spilling out of bounds."

Your walk says 0 hard findings — and both are true, because the walk runs as
the SMOKE account against sparse fixtures, and the owner lives on a team with
448 open tickets, 131 accounts and data-heavy records. The gaps are on
data-rich screens the walk never sees.

So: **run the walk — and your eyeball pass — signed in as `alaap@kwapso.com`**
(the same admin door; the key mints a code for any account). Sweep the screens
his data makes heavy: tickets, accounts (both tabs), an account record with
many contacts, the knowledge base with real sources, meetings, stories, the
app record's Impact tab. Fix what the kit owns in the kit (off the tag),
app-side chrome app-side. Everything you find that you don't fix, list with a
measurement, same discipline as your NAV-EDGE note.

## 5 · The brimba-* deploy you saw

Not this repo and not this lane — flagged to the owner; nothing for you to do.
